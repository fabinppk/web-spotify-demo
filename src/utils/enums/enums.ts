export enum MainContent {
  PROFILE = "PROFILE",
  SETTINGS = "SETTINGS",
  ALBUMS = "ALBUMS",
  PLAYLISTS = "PLAYLISTS",
  TRACKS = "TRACKS",
  BROWSE = "BROWSE",
  PLAYER = "PLAYER",
}

export enum Theme {
  Dark = "dark",
  Light = "light",
}

export enum ThemeActionType {
  Toggle = "TOGGLE_THEME",
  Set = "SET_THEME",
}
